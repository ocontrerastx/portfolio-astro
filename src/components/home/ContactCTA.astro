<section class="contact-cta">
  <div class="page-container">
    <div class="cta-card">
      <h2 class="cta-title">Need a hybrid partner?</h2>
      <p class="cta-description">
        I’m currently taking on select projects where product strategy, UX
        design, and development need to move in lockstep.
      </p>
      <div class="cta-actions">
        <a href="mailto:hello@wally.contreras" class="cta-link primary">
          Let's Talk
        </a>
        <a href="/resume" class="cta-link secondary"> Download Resume </a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-cta {
    padding: 8rem 0 10rem;
  }

  .cta-card {
    /* Matches the soft, tinted background from your reference */
    background-color: #f9f3f1;
    padding: 6rem 2rem;
    border-radius: 24px;
    text-align: center;
    max-width: 64rem;
    margin: 0 auto;
    /* Subtle border to define the box without high contrast */
    border: 1px solid rgba(215, 91, 66, 0.1);
  }

  .cta-title {
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    font-weight: 900;
    color: var(--text-main);
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
  }

  .cta-description {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--text-muted);
    max-width: 36rem;
    margin: 0 auto 3.5rem;
  }

  .cta-actions {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
  }

  @media (max-width: 768px) {
    .cta-card {
      padding: 4rem 1.5rem;
    }
    .cta-actions {
      flex-direction: column;
      align-items: center;
    }
    .cta-actions a {
      width: 100%;
    }
  }
</style>
